BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
# Optional. OAuth redirect URI defaults to BACKEND_URL + /auth/callback for both Discord and Twitch.
# Set only if your public callback URL differs (e.g. behind a proxy). Example: https://api.example.com/auth/callback
# DISCORD_REDIRECT_URI=
# TWITCH_REDIRECT_URI=
# Discord Bot (slash commands): create a Bot in the same Discord app, copy token and public key
# DISCORD_BOT_TOKEN=
# DISCORD_PUBLIC_KEY=
# Twitch OAuth (login + bind): create app at https://dev.twitch.tv/console/apps
# Both Discord and Twitch use the same callback: BACKEND_URL + /auth/callback (provider inferred from state).
# Twitch only accepts HTTPS redirect URIs; for local dev use an HTTPS tunnel (e.g. ngrok).
# TWITCH_CLIENT_ID=
# TWITCH_CLIENT_SECRET=
# Optional override if callback URL differs from BACKEND_URL + /auth/callback (e.g. ngrok URL for local Twitch dev)
# TWITCH_REDIRECT_URI=https://your-app.ngrok-free.app/auth/callback
# Admin: also support Twitch user IDs for /admin
# ADMIN_TWITCH_IDS=
# Optional: admin API key for server-to-server or script access (X-Admin-API-Key or Authorization: Bearer <key>)
# ADMIN_API_KEY=
SECRET_KEY=change-me-in-production
ACAPY_ADMIN_URL=http://localhost:8020
ACAPY_ADMIN_API_KEY=
INNKEEPER_ID=
INNKEEPER_KEY=
ADMIN_DISCORD_IDS=
# Comma-separated Discord user IDs; these users see the Admin button and can access /admin. Get your ID: Discord → Settings → Advanced → Developer Mode ON → right-click your avatar → Copy User ID.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tritone_cards
# Card/set images: stored on disk (UPLOAD_DIR), not in DB. Persist across deploys (volume or bind mount).
# UPLOAD_DIR=uploads
# Optional: serve the SPA from the backend (single-origin). Set to path of frontend build (must contain index.html and _app/).
# Used by Dockerfile.combined; or set when running backend with frontend build mounted/copied in.
# FRONTEND_BUILD_DIR=/app/frontend-build
# Optional: OCR microservice URL for card image analysis (Admin "Analyze" suggested fields). If unset, analyze returns metadata only.
# With docker-compose: OCR_SERVICE_URL=http://ocr:8001 (run `docker compose up -d ocr`).
# Local dev: run ocr-service (e.g. cd ocr-service && uvicorn main:app --port 8001) and set OCR_SERVICE_URL=http://localhost:8001
# OCR_SERVICE_URL=http://localhost:8001
# OCR uses the same key as admin API (ADMIN_API_KEY above): backend sends it as X-OCR-API-Key when calling OCR.
# WebAuthn (passkeys): optional; rp_id and origin default from FRONTEND_URL
# WEBAUTHN_RP_ID=localhost
# WEBAUTHN_RP_NAME=Brutality Cards
# WEBAUTHN_ORIGIN=http://localhost:5175
# Security: rate limiting (per IP). Enable with RATE_LIMIT_ENABLED=1
# RATE_LIMIT_ENABLED=0
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_WINDOW_SECONDS=60
